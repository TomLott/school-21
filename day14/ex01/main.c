#include <unistd.h>
#include <stdio.h>

#include <stdio.h>
void	print_arr(int *tab, int length)
{
	int i;
	i = 0;
	while (i < length)
	{
		printf("%d ", tab[i]);
		i++;
	}
}
int	square(int a)
{
	return a * a;
}
int *ft_map(int *tab, int length, int (*f)(int));
int main()
{
	int arr[4] = {1, 2, 3, 4};
	int a;
	printf("%d",a);
}
